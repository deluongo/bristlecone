# DEFERRED

Deferrals require a concrete re-entry trigger. No trigger, no entry.

| ID | Deferred item | Re-entry trigger |
|---|---|---|
| DEF-001 | Public-CI private-context denylist (currently a local gitignored gate, because a public denylist would leak the names it protects) | Any hygiene near-miss caught by the local gate, OR the repo gains a second regular committer |
| DEF-002 | Competitor response: a major council tool ships git-persistent, attributed records | Observed via release notes/stars → open a Class-A deliberation: pivot to interop (import their transcripts as records) vs differentiate |
| DEF-003 | PR-per-milestone vs main-only, revisited | The public flip (M1), OR the first external contributor PR |
| DEF-004 | Sponsors button | C3 checkpoint (2026-11-18) passes AND a concrete cost exists (e.g. domain renewal) |
| DEF-005 | Token-usage capture for subscription-CLI lanes (M0 records show $0.00 rows without token counts) | M2 `ask` implementation — capture usage wherever the provider reports it |
