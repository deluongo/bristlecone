# Process reflections

Fortnightly scoreboard (first entry due ~2026-09-04): fixes-that-took from the prior entry, re-asks of granted authorizations (target 0), FUSED session count, burn trend, and one process change proposed as a record if warranted.

## Running log (raw material for the fortnightly entries)

- 2026-08-21 (m1-verify session): **process bug** — the M0-S3 session ran the m0-verification deliberation over three zero-cost lanes but added no $0.00 ledger rows, against TREASURY.md's "zero-cost lanes appear when used" contract. Repaired by late-recorded rows (marked †) in the same ledger. Class: bookkeeping omission; no spend involved.
- 2026-08-21 (M2-S2 session): **process bug — local gate weaker than CI gate.** The session ran xenon locally with `--max-modules B` while ci.yml enforces `--max-modules A`, so main went red for one commit (`laneconfig.py` module-ranked B on average complexity; hygiene and pages stayed green). Fixed within the session by decomposing the parser into smaller helpers (avg 5.29 → 3.73) — no gate was weakened. Rule going forward: pre-ship gate runs must copy the exact commands out of ci.yml, not reconstruct them from memory.
- 2026-08-23 (M2-S4 session): **gate gap found and fixed — ship.sh's denylist/credential greps scanned only tracked files.** The gates run before `git add`, and `git grep` ignores untracked files, so a denylisted term inside a brand-new file would pass its first ship untouched and only block the *next* one. Surfaced live: the operator named a sister project in INBOX.md (tracked → gate fired, correctly), while the same name quoted into a new record file was invisible to the gate. Fixed by adding `--untracked` to both grep gates; the term itself was bracket-redacted in both files. Two lessons kept: the privacy boundary binds even operator-typed text, and a gate that scans "the repo" must scan what is *about to become* the repo.
- 2026-08-27 (M2-S8 session): **process bug (repeat of the M0-S3 class)** — the M2-S7 session ran a real `ask --lanes qwen` during the onboarding rehearsal but added no $0.00 ledger row, against TREASURY.md's "zero-cost lanes appear when used" contract; plausibly because the target record lived in the scratch repo, but the lane use was project work. Repaired by a late-recorded † row (M2-S8), same mechanism as the first occurrence. Second instance of the same omission class → worth a cheap structural fix rather than vigilance: candidate for the fortnightly entry — have `ask` print a ready-to-paste ledger row with its lane summary.
- 2026-08-27 (M2-S9 session): **lesson, not a bug — outward-facing prose needs the vision the archive already holds.** The operator ruled the M2-S8 outreach drafts visionless ("elevator pitch sucks... this feels mechanical") despite their factual soundness; the founding-thesis record contained the exact framing the drafts should have led with. Kept rule: any deliverable aimed at strangers (outreach, announce, participate-page) gets drafted against the founding-thesis record first, and gets a cross-vendor critique pass before it's called ready — this session's pass ruled the first rewrite NOT-READY 3/3 and caught overclaims plus a disclosure link that would have 404'd at post time.
- 2026-08-29 (M2-S11 session): **process change adopted by operator ruling — the outbound-text pin.** M2-S10 disclosed unprompted that the posted kandev text had no second formal review round and offered a pin; the operator took it ("pin outbound text for a second review round and probably at least get a majority consensus"). Now a CLAUDE.md hard rule with both halves of a loop written down: convergence (majority `ready` + no new blocking finding) and a fuse (Class-B round cap; a second failed re-vote holds the text and goes to the operator). Two mechanics notes: (a) `ask` declines duplicate labels, so a second round on the same open record needs distinct lane names — solved with `lanes-round2.toml` (same lanes, `-r2` names) rather than a code change; DEF-006 holds the structural fix (`ask --round N`). (b) The local ollama daemon was not running at session start (lane would have failed as `failed:*`, honestly, but silently shrinking the quorum) — a lane-availability check belongs at the top of any session that plans a fan-out; started by hand this time.
- 2026-08-21 (m1-verify session): **test rewrite, class (b) invariant drift** — `test_real_archive_renders_with_operator_dissent_first_class` hardcoded the archive at 9 records; adding the 10th (the M1 verification record itself) broke it. The real invariant is "index + one page per record, never fewer than the founding 9"; test rewritten to that. An append-only archive's tests must not pin its size.
