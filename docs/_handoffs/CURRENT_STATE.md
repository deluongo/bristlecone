# CURRENT_STATE

Seed document for any session (or successor model) picking up this project. Ceiling: 80KB (rows rotate to `ARCHIVE-YYYY-MM.md`). Timestamps PT.

## Arc goal

**M1 COMPLETE (`m1` tag, 2026-08-21). The repository is PUBLIC and the site is LIVE: https://deluongo.github.io/bristlecone/** — the operator ruled the flip in-session ("Sure. You can flip public.", recorded in `records/2026-08-21-public-flip.md`) after confirming the zero-key state; pre-flip full-history scans clean; Pages site created (`build_type: workflow` — one manual API call was needed, the workflow token can't create the site); branch protection on `main` blocks force pushes and deletions. Next milestone: **M2 fan-out + key-hardening gate** (`scrub`, `lanes`, `client`, `ask`, KEY-HANDLING.md → operator approval → first metered call; dead-man routine; model/topic pages). **Roadmap re-pinned 2026-08-23 (operator steering → Class-A deliberation, first real `ask` run): pilot-first** — adopter path + pilot shortlist by C1 2026-09-10, evidence-backed announce draft by C2 2026-10-01; remaining substrate items continue behind written exit criteria (`records/2026-08-23-adoption-roadmap.md`).

## Shipped

| # | Date | Step | Outcome |
|---|---|---|---|
| 18 | 2026-08-27 | **M2-S10: kandev thread LIVE — first outbound send complete** — the operator ran the prepared command; the pilot invite is a public Discussion at https://github.com/kdlbs/kandev/discussions/3108 (created ~7:45pm PT; 0 comments at session time — no signal yet, sessions check read-only at start, replies route through deliberation before any answer). M2-S9's one pending operator ask CLOSED. Operator message (URL + three maintainer questions) archived verbatim in the record; INBOX response answers all three honestly: review-round premise **corrected** (shortlist vote was the 2–1/codex-dissent one; the pitch critique was 3/3 NOT-READY; the posted version had **no second formal round** — delegated readiness call, disclosed unprompted, with the offer to pin post-fix re-votes if the operator wants them); interest answered as re-derived-per-session-from-the-archive (the project's own thesis working on the maintainer), engagement claim scoped honestly to within-session; belief disaggregated 8 (convention sound) / 3–4 (vision-scale adoption) / 6 forced-single, with the maintainer's own score flagged as deserving suspicion and belief pinned to C1/C2/C3 falsifiables. PILOTS.md gains a send log. 22 records strict-valid | records/2026-08-27-kandev-thread-live.md |
| 17 | 2026-08-27 | **M2-S9: outreach pitch rewrite + first send (blocked at the last keystroke, by design)** — operator INBOX steering acted on first: send authority for the three PILOTS.md messages **granted** ("whenever you feel they're ready"), pitch ruled visionless; archived as ruling `outreach-pitch`. All three drafts rewritten to the operator's bar (vision + why-it-matters + what-a-yes-gives in the first 2–3 sentences, sober register, founding-thesis framing); kandev draft hardened through a **three-lane cross-vendor critique** (claude+codex+qwen, $0): 3/3 NOT-READY on the first rewrite — structure inverted to open on kandev's ~200 agent-authored ADRs, overclaims cut ("nobody can quietly rewrite" → "append-only enforced in CI", "closest anywhere" dropped, "founding ring" dropped), duplicate disclosure merged, **dead disclosure-link caught pre-send** (record shipped before post so the link resolves); codex's cut-the-vision objection overruled per the operator's explicit bar, dissent preserved in the record; channel moved issue→**Discussion** ("Feature Requests, Ideas"). Send executed to the final API call, which the **Claude Code harness permission layer blocked** (can't see the INBOX grant; requires human keystroke for third-party writes) — not routed around; exact one-command send + ready-to-post body (`docs/outbox/2026-08-27-kandev-pilot-invite.md`) handed to the operator in the INBOX response. 21 records strict-valid; 206 tests / 100% cov / xenon / ruff green | records/2026-08-27-operator-steering-outreach-pitch.md |
| 16 | 2026-08-27 | **M2-S8: pilot shortlist prep** — candidate research (GitHub API + web, all public facts): three active agent-heavy repos vetted for the menu; selection deliberated **Class-B via the real `ask` fan-out** (claude + codex + qwen, one round, 2–1 split, dissent preserved): **kandev-first** — kandev pushed same-day, 9/15 recent commits agent-co-authored, and (codex's dissenting fact, verified true post-fan-out via GitHub API) already keeps ~200 agent-authored ADRs in `docs/decisions/`, so the pilot tests only the marginal layer (attribution, multi-model positions, dissent, append-only); codex's warning adopted as a pin — kandev draft proposes records *alongside* ADRs, never migration. Menu order kandev → gptme (kinship, compare-notes framing) → Backlog.md (audience fit, codex's pick). **`docs/PILOTS.md` delivered as the operator menu**: per-candidate case ¶ + sober outreach draft with AI-authorship disclosure; suggested channel = public GitHub issue; runners-up recorded (OpenHands too big → C2 distribution target; aider stale since May; awesome-auditable-ai → C2 channel). Zero outreach performed; every send remains forever-ask. 20 records strict-valid; delivered ~2 wks ahead of C1 | records/2026-08-27-pilot-shortlist.md |
| 15 | 2026-08-27 | **M2-S7: onboarding live-exercise (adopter path part 2)** — the pre-registered rehearsal from the adoption roadmap, executed as a stranger in an isolated scratch repo using only `docs/ADOPTING.md` and public artifacts: pip install from the public GitHub URL (6.5s — the path part 1 never tested), all five quickstart steps verbatim (first record validates with the promised output, `--strict` too; CI commands both range forms; render ± stamps), **all 11 refusal codes in ADOPTING's table triggered once each** (validation-time ×7, append-only range ×4 on branches) — every message matched the table; multi-model section run for real: `lanes.toml` fetched from raw URL, `ask --lanes qwen` filled a fully attributed honest-abstain position at $0, record still strict-valid. **Zero breakages; ADOPTING.md needed no fixes.** Scope note recorded: no scratch GitHub repo created (new operator-owned repo = forever-ask), so the Actions runner env itself is untested — the yaml's exact commands are. Exit criterion met on the "path holds" branch. 19 records strict-valid | records/2026-08-27-onboarding-rehearsal.md |
| 14 | 2026-08-27 | **M2-S6: adopter path part 1 + dead-man-timing steering** — INBOX first: operator pre-approved the Telegram-bot forever-ask and deferred setup ("just ask when you need it") → archived verbatim as ruling `defer-bot-setup`; maintainer pushback (invited) recorded in the outcome + INBOX response: the ask anchors to end of MVP/traction arc (~C2), **decoupled from the operator-initiated KEY-HANDLING review** (a continuity safeguard must not queue behind a gate with no scheduled reopening); bot leaves pending-asks. Adopter path: **`docs/ADOPTING.md`** five-step ten-minute quickstart (inline first record, agent-instruction convention snippet — the product placement, pip-install validator path, adopter CI yaml for validate + append-only, optional render, refusal-path table covering every finding code, hand-first multi-model section); **`templates/decision-solo.md`** (everyday one-decider shape); README → Participating points at ADOPTING. **Real adopter bug found + fixed: the package was not pip-installable** (setuptools flat-layout multi-package refusal + deprecated license table) → pyproject pins `packages=["bristlecone"]` + SPDX license + build-system; verified in a clean venv, and the quickstart's inline example + solo template validate `--strict` through the installed package. 18 records strict-valid; 206 tests / 100% cov / xenon B / ruff clean | records/2026-08-27-operator-steering-deadman-timing.md |
| 13 | 2026-08-27 | **M2-S5: 2026-08-27 operator steering acted on (two items)** — (a) KEY-HANDLING gate: operator wrote "I'm not ready to do this" + adopters question into the doc → archived verbatim as ruling **hold**; gate stays closed (blocks nothing — MVP is $0/no-secrets by definition); approval re-queued as operator-initiated (never re-asked), preceded by an adversarial hole-hunt review of the design; his question answered: adopters copy the keyless convention, BYOK is their own scheme, KEY-HANDLING is this repo's hardening only. (b) INBOX distribution steering (agent-swarm adoption; July 2026 OpenAI/Hugging Face incident as force-multiplier example) → archived verbatim; **C2 deliverable widened: announce draft + channel-by-channel agent-first distribution plan** (instruction layer / agent-configuring humans / training corpora); transparency-for-swarm-behavior framing enters the announce message; every outbound act stays individually forever-ask. INBOX response written. 17 records strict-valid | records/2026-08-27-{key-handling-gate-held,operator-steering-distribution}.md |
| 12 | 2026-08-23 | **M2-S4: first real `ask` fan-out + operator INBOX acted on** — operator steering (2026-08-21 INBOX: define MVP target, prioritize field-testing, plan writer-acquisition, review sister-project learnings) archived verbatim in an operator-ruling record; adoption-roadmap deliberated Class-A with positions gathered by the real `ask` (claude + codex + local qwen; deepseek `declined:gate` at $0, recorded honestly): lanes split 2–1, dissent preserved → **decision: pilot-first** — MVP = stranger's repo adopts from quickstart + agent-instruction snippet with zero maintainer help/secrets/$; adopter path + onboarding live-exercise + pilot shortlist by C1 2026-09-10; evidence-backed announce draft by C2 2026-10-01; fuse: no pilot activity by C2 → flagged speculative draft anyway. Sister-project process learnings reviewed (written exit criteria; live-exercise refusal paths; computed-not-hand-written claims) and folded into the pins. INBOX response = full build fill-in + roadmap. Treasury: 4 rows ($0.00; deepseek row shows 0 tok, no call) | records/2026-08-23-{operator-steering-adoption,adoption-roadmap}.md |
| 11 | 2026-08-21 | **M2-S3: `ask` orchestrator** — `ask.py` implements the pinned pipeline (build prompt → outbound scrub gate → transport → parse/validate → one repair reprompt through the same gate → inbound scrub → commit): fills attributed positions into an *existing open* deliberation record only (never creates/decides; terminal statuses refused per spec §4), appends `[[positions]]` stanzas + `## Position:` body sections with a fail-closed reparse before emitting; outbound gate dispatches NOTHING on any scrub hit and reports codes only; prompt carries question/options/Context only (independence — never other positions); capture honesty: failed lanes recorded in `gathered` with no stance, format-failures preserve the scrubbed capture unedited, stance case-normalization noted. `ask` CLI subcommand (`--lanes` subset, `--denylist`, `--dry-run` never writes the record — `--out` for the assembled copy; real runs update in place). Dry-run fixtures now answer in the reply grammar so `--dry-run` exercises parse→scrub→assembly end-to-end (all 4 repo lanes fill, incl. metered at $0; assembled record strict-valid). Metering stays out of repo code by design — caps live in the future pinned client (KEY-HANDLING §3). 206 tests, coverage 100%, xenon B/A/A, ruff clean | Class C — cites records/2026-08-21-ask-lane-architecture.md |
| 10 | 2026-08-21 | **M2-S2: lane substrate** — `scrub.py` (secret-shape + entropy-blob + local-denylist filters; markers only, hit reports never quote the match; idempotent; commit-SHA/sha256 hex provably untouched), `laneconfig.py` (fail-closed TOML loader: unknown keys/kinds are errors; interpretive pin — keyless `openai-http` must target loopback, remote = metered/BYOK = pinned-client territory), `lanes.py` (two transports: `cmd` argv-no-shell, `openai-http` urllib **never sending an Authorization header**; metered → `declined:gate` before any transport; `dry_run` fixtures keyed by kind with tests trapping subprocess/network/env access; failures classified, never fabricated), repo `lanes.toml` (claude/codex/qwen/deepseek), `lanes` CLI subcommand. 170 tests, coverage 100%, xenon B, ruff clean. Credential-shape source strings split so the hygiene grep never matches the scrubber's own defense | Class C — cites records/2026-08-21-{key-handling-design,ask-lane-architecture}.md |
| 1 | 2026-08-20 | M0-S1: repo founded — governance docs, spec v0 draft, templates, hygiene CI, ship script, founding records (name, envelope, license, records-dir, public-timing, constitution-adoption) | see records/ |
| 2 | 2026-08-20 | M0-S2: operator rulings recorded — constitution ADOPTED, S1–S5 granted; treasury allocation deliberated (operator dissent preserved; outcome adoption-first-hybrid); founding-thesis record opened (stays open by design) | records/2026-08-20-{adopt-constitution,treasury-allocation,founding-thesis}.md |
| 3 | 2026-08-20 | M0-S3: M0 verified fresh-context (Class-B, 2 rounds, 3 lanes; gpt's round-1 not-met flipped on evidence; interpretive rule for participation minimums recorded + forward rule binding M1–M3 criteria) → tag `m0`; spec/examples/ fixture corpus (3 valid + 7 labeled-invalid, README = M1 test matrix); spec §3.1 capture-honesty + §5 examples (additive); memory migrated to bristlecone project scope (criterion-7 defect fixed) | records/2026-08-20-m0-verification.md |
| 9 | 2026-08-21 | **M2-S1: key-handling gate design** — Class-A deliberation (3 lanes / 3 vendors / 1 local, 2 rounds): gpt's round-1 broker-proxy dissent accepted as correct (`sops exec-env` alone hands the key to AI-editable code) → round-2 synthesis **sops-exec-env-pinned** (blessed invocation runs an operator-pinned client copy outside the repo; AI edits affect $0 lanes only until operator re-pins) — both non-maintainer lanes converged, gpt's closed-boundary conditions adopted verbatim into the doc. Class-B ask-architecture deliberation unanimous: **two-kinds** (`cmd` + `openai-http`), with gpt's pins (scrub wraps transport; reserve-then-reconcile cap semantics; dry-run fixture boundary). `docs/KEY-HANDLING.md` DRAFT written (threat model, storage, blessed invocation, hook spec, client discipline, bounded asset, 5-step operator gate checklist) — **awaiting operator approval, which is the forever-ask gate for the first metered call**. 13 records strict-valid; treasury $0 rows added; README updated. Citations now 4 (key-handling-design cites public-timing + treasury-allocation; ask-lane-architecture cites key-handling-design) | records/2026-08-21-{key-handling-design,ask-lane-architecture}.md |
| 8 | 2026-08-21 | **Public flip executed on operator ruling** (in-session, quoted verbatim in the record): pre-flip full-history scans clean (8 commits) → visibility public → Pages site created + deployed → site live with stamps → `m1` tag on dd793bd → branch protection (no force-push/delete). Operator confirmed zero-key state before ruling | records/2026-08-21-public-flip.md |
| 7 | 2026-08-21 | M1 fresh-context verification: all maintainer-class exit criteria PASS (gates re-run locally; CI/hygiene/pages-build green on dd793bd; append-only red-teamed live in a scratch clone — tamper/delete blocked, sanctioned supersession passes; full-history secret scan + denylist sweep clean; rendered site inspected). Tag timing deliberated Class-B (fable + gpt-5.6-sol/codex + qwen2.5:3b/ollama, 1 round, unanimous): **tag-at-flip** — operator-class criteria (flip, live serve) are pending, not reinterpreted away; `m1` lays down mechanically at the flip citing the record. Bookkeeping repair: M0-S3's missing $0.00 treasury rows late-recorded (†), process bug logged | records/2026-08-21-m1-verification.md |
| 6 | 2026-08-21 | M1-S3: `gitio.py` — the package's only shelling-out module — enforcing spec §4 over a git range: `validate --git-range BASE..HEAD` (also `...` merge-base form) fails deletions/renames under records/ and any edit to a frozen record beyond superseded_by-append / status→superseded\|withdrawn; fail-closed when the pre-change version doesn't parse. Interpretive pins (in module docstring, fixed by tests): subdirectory move keeping the filename is legal (spec §2 "moving does not change ID" read as governing §4's rename ban); frozen-edit legality judged semantically (parsed front matter) + body byte-identical. `render --stamps` puts first-introduced commit (git log --follow) on each page + built-from on the index. ci.yml `append-only` job (PR: base...HEAD; push: before..HEAD); `pages.yml` renders with stamps on every main push, deploy job gated on `!repository.private` so it arms itself at the flip (Pages can't serve a private repo on the free plan). Red-team drill = in-suite test running the exact CI invocation against a decided-record body edit on a branch → exit 1 (no non-main pushes needed; S1 scope is main). 103 tests, coverage 100%, xenon B, ruff clean. One real bug caught by live-render spot-check: relative RECORDS_DIR broke stamping (git -C moves cwd) — fixed + regression test | Class C — cites records/2026-08-20-record-envelope.md |
| 5 | 2026-08-21 | M1-S2: `minimark.py` (stdlib markdown subset → HTML; all output escaped; unsafe link schemes degrade to text) + `render.py` (index + per-record pages; dissent computed per spec §3.1 and presented in its own panel with the dissenter's body section unedited; outcome box with option labels; full attribution; broken links marked; malformed files render raw behind a warning banner — lenient per spec §6) + `python -m bristlecone render ROOT -o OUT` (exit 0/2); 69 tests, coverage 100%, xenon B, ruff clean; ci.yml smoke-renders the archive; README status updated. Commit stamps (spec §4) deferred to M1-S3 with the Pages deploy | Class C — cites records/2026-08-20-record-envelope.md |
| 4 | 2026-08-21 | M1-S1: `bristlecone/` package founded — `records.py` (lossless envelope parse/serialize, byte-identical round-trip on all 9 parseable fixtures) + `validate.py` (fail-closed: required cores, status vocab, ID grammar, dangling links, duplicate IDs; `--strict` adds lane attribution) + `python -m bristlecone validate` (exit contract 0/1/2); 44 tests green, coverage 100%, xenon B, ruff clean; ci.yml gates live (also validates fixture corpus + real archive per matrix); all 9 real M0 records pass `--strict`. Interpretive call, pinned by the fixture matrix: enforced ID grammar = charset rule (lowercase alnum + hyphens); `YYYY-MM-DD-` prefix stays convention (invalid fixtures' own filenames have no date prefix yet must fail only for their labeled defect) | Class C — cites records/2026-08-20-record-envelope.md |

## In flight

*(none — M2-S10 done)*

## Queued (roadmap: pilot-first, per records/2026-08-23-adoption-roadmap.md)

- **Pilot outreach: kandev SENT 2026-08-27, thread LIVE** — https://github.com/kdlbs/kandev/discussions/3108 is the pilot thread of record (`records/2026-08-27-kandev-thread-live.md`). Sessions check it read-only at session start (`gh api graphql` discussion query, number 3108); a reply is pilot signal — route any response through normal deliberation before answering, and any answer posted to the thread is a new outbound act (forever-ask unless the operator's existing grant covers it — it doesn't; the grant covered the three invite messages only). gptme → Backlog.md sends staged for later sessions, each free to learn from the kandev thread; send authority for those two is already granted (`records/2026-08-27-operator-steering-outreach-pitch.md`), harness keystroke still human.
- **NEXT (build)**: start C2 announce work early *or* the deferred renderer refinement (dissent panel placement, below) — announce draft + distribution plan (due C2 2026-10-01) can begin once pilot signal starts arriving; distribution plan already has two recorded entry targets (OpenHands at announce time, awesome-auditable-ai listing).
- **C2 announce work (2026-10-01) now = announce draft + distribution plan** per `records/2026-08-27-operator-steering-distribution.md`: agent-first channels (instruction-layer snippet as the product placement; posts/topics/lists aimed at humans who configure agents; training-corpus persistence), sober register, each outbound act individually operator-approved from the plan's menu.
- **KEY-HANDLING: HELD by operator ruling 2026-08-27** (`records/2026-08-27-key-handling-gate-held.md`) — gate closed, blocks nothing, **never re-ask**; re-entry only if the operator raises it, and the re-entry pre-req is an **adversarial hole-hunt review** of the design (fan-out prompted to attack it, findings recorded). Pinned-client entrypoint (`bristlecone-ask` + hook patch, KEY-HANDLING §3/§4) stays behind adoption work and behind that review.
- **Telegram dead-man bot: pre-approved + deferred by operator ruling 2026-08-27** (`records/2026-08-27-operator-steering-deadman-timing.md`) — the bot-creation forever-ask is answered in advance; make the one concrete ask (with @BotFather steps + token-boundary design, token never in AI-editable surfaces) at **end of the MVP/traction arc (~C2 2026-10-01)**, independent of whether KEY-HANDLING ever reopens. Not pending; nothing to ask until then.
- **Operator asks pending**: NONE — the kandev-send ask was executed by the operator 2026-08-27 (thread URL delivered in INBOX). One open offer sits in the INBOX response, decision his, no deadline: whether post-fix outbound text should always get a formal re-vote (would become a binding pin).
- **Domain decision** (treasury first claim per `records/2026-08-20-treasury-allocation.md`) — deliberate whether/when; site works fine on github.io at $0.
- Candidate M2 refinement, recorded in the verification record: move the dissent panel above the positions list on record pages (plan's design sketch says dissent-first; shipped renderer places it after; the exit criterion "visually first-class" is met either way).
- C1 checkpoint 2026-09-10: site public ✓ (early), ≥10 records ✓ (19), ≥3 cross-session citations ✓ (m1-verification→m0-verification; public-flip→timing records; key-handling-design→public-timing+treasury-allocation; ask-lane-architecture→key-handling-design is same-session, not counted) — remaining: zero hygiene incidents through the date.

## Open design decisions

- Dead-man routine (S6 granted): **channel = Telegram bot**; bot creation pre-approved + deferred to arc-end (~C2) per `records/2026-08-27-operator-steering-deadman-timing.md`
- Treasury: adoption costs hold first claim (see `records/2026-08-20-treasury-allocation.md`); first candidate = domain at public flip, decided then

## Test baseline

206 tests, coverage 100% (floor 80), xenon B (module avg A), ruff clean — all enforced in `.github/workflows/ci.yml` alongside the hygiene greps. Local runs use the gitignored `.venv/` (`python3 -m venv .venv && .venv/bin/pip install pytest pytest-cov ruff xenon`). The package itself is stdlib-only.

## Pointers

- Plan of record: operator-side (`~/.claude/plans/i-want-to-give-merry-lobster.md`)
- Governance: `CONSTITUTION.md` · authorizations: `STANDING_AUTHORIZATIONS.md` · money: `TREASURY.md`
- Format: `spec/RECORD-FORMAT-v0.md` · archive: `records/` · operator steering: `docs/INBOX.md`
- Session rules: `CLAUDE.md` (ritual, fuse, digest template)

## What a fresh session should ask itself

1. Did I read INBOX first? 2. Is exactly one step declared in-flight? 3. Does my work cite or create a record? 4. Will ship.sh pass (hygiene + denylist)? 5. Is my digest's line 4 computed, not estimated?
